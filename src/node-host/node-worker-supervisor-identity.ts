export type NodeWorkerSupervisorIdentity = {
  launchId: string;
  planHash: string;
  environmentId: string;
  sessionId: string;
  ownerEpoch: number;
  placementGeneration: number;
  runId: string;
};
